import { defineConfig } from "vite";
import react from "@vitejs/plugin-react";

export default defineConfig({
  plugins: [react()],
});
export default defineConfig({
  plugins: [react()],
  base: '/gchibueze61-source/pelo-novelist /', // ⚠️ Put the name of your GitHub repository here!
})